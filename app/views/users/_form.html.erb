<%= form_with(model: user, local: true, html: { multipart: true }) do |form| %>
  <% if user.errors.any? %>
    <div class="alert alert-danger">
      <b><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</b>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, id: :user_name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :discord_uid %>
    <%= form.text_field :discord_uid, id: :user_discord_uid, class: 'form-control' %>
  </div>

  <div class="checkbox">
    <%= form.label :admin do |label| %>
      <%= form.check_box :admin, id: :user_admin %>
      Admin?
    <% end %>
  </div>
  
  <div class="checkbox">
    <%= label_tag :remove_avatar do |label| %>
      <%= check_box_tag :remove_avatar %> Remove avatar?
    <% end %>
  </div>
  
  <div class="form-group">
    <%= form.label :avatar %>
    <%= form.file_field :avatar %>
  </div>

  <%= form.submit class: 'btn btn-primary' %>
<% end %>